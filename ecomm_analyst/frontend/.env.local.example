# Copy to .env.local in this folder (gitignored). Restart `npm run dev` after edits.
# `NEXT_PUBLIC_*` is inlined at build time for static export — set in Cloudflare Pages env for production.

# FastAPI base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:8000
