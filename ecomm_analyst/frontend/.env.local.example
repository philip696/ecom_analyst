# Copy to .env.local (not committed). Rebuild Next after changes.
# API for JSON + /images — use your Cloudflare Worker URL in production builds via Pages env vars.
NEXT_PUBLIC_API_URL=http://localhost:8000
