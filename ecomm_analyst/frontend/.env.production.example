# Cloudflare Pages → Settings → Environment variables (Production + Preview).
# Use your Worker API origin (required for build when CF_PAGES=1). No trailing slash.
NEXT_PUBLIC_API_URL=https://ecom-analyst.your-subdomain.workers.dev
